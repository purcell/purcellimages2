(executable
 (public_name site)
 (name site)
 (modules site db templates)
 (libraries dream dream-html caqti caqti-driver-postgresql ppx_blob)
 (preprocess (pps lwt_ppx ppx_blob))
 (preprocessor_deps
  (file site.css)
  (file nav.js))
)

; (rule
;  (targets templates.ml)
;  (deps templates.html.ml)
;  (action (run dream_eml %{deps} --workspace %{workspace_root})))
